# FCR Reminder Daemon

FCR Reminder is the background companion app for Full Calendar Remastered.

The current source of truth lives in the `docs/` directory:

* `docs/user/`
* `docs/architecture/`
* `docs/developer/`

For implementation details, runtime behavior, and verification workflow, use those documents instead of this front page.
